description: >
  Sample description
# What will this job do?

docker:
  - image: cimg/base:current

parameters:
  to:
    type: string
    default: "World"
    description: "Hello to whom?"
steps:
  - greet:
      to: << parameters.to >>
